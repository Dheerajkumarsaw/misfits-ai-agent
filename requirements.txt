fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
chromadb>=0.5.0
openai==1.51.0
pandas>=2.2.0
numpy<2.0,>=1.26.0
sentence-transformers>=3.0.0
requests==2.31.0
python-multipart==0.0.6
typing-extensions>=4.5.0
asyncio>=3.4.3
# Standard library packages (no need to install):
# - pickle, subprocess, sys, json, datetime, re, io, typing, uuid, os, threading, time, concurrent.futures
# cors==1.0.1  # Not needed - CORS is handled by fastapi