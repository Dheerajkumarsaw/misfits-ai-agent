apiVersion: v1
kind: ConfigMap
metadata:
  name: meetup-api-config
  labels:
    app: meetup-api
data:
  PYTHONUNBUFFERED: "1"
  CHROMA_HOST: "43.205.192.16"
  CHROMA_PORT: "8000"
  LOG_LEVEL: "info"
---
apiVersion: v1
kind: Secret
metadata:
  name: meetup-api-secrets
  labels:
    app: meetup-api
type: Opaque
data:
  # Add any secrets here (base64 encoded)
  # Example: API keys, database passwords, etc.
  openai_api_key: ""  # Add your OpenAI API key here (base64 encoded)